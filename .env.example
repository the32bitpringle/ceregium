# Optional. Without this key Ceregium uses its deterministic wellbeing analyzer.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional public URL used in OpenRouter request metadata.
APP_URL=http://localhost:3000
